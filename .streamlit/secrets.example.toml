LLM_PROVIDER = "Gemini"
GEMINI_MODEL = "gemini-3.5-flash"
GOOGLE_API_KEY = "paste-your-google-api-key-here"

# Ollama is for local runs only. Streamlit Community Cloud does not provide
# a local Ollama server, so use Gemini for the deployed app.
OLLAMA_MODEL = "gemma3:4b"
OLLAMA_NUM_GPU = "0"
